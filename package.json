{
  "name": "cf-worker-mihomo",
  "version": "1.2.2",
  "private": true,
  "license": "MIT",
  "scripts": {
    "deploy:workers": "wrangler deploy --yes",
    "deploy": "vercel --prod --yes",
    "dev:workers": "wrangler dev src/_worker.js --live-reload",
    "dev": "vercel dev --yes",
    "build": "node esbuild.js",
    "start:workers": "wrangler dev",
    "start": "vercel dev --yes",
    "lint": "prettier --write \"**/*.js\""
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.8.47",
    "esbuild": "^0.25.8",
    "prettier": "^3.6.2",
    "vercel": "^44.2.7",
    "wrangler": "^4.22.0"
  },
  "dependencies": {
    "yaml": "^2.8.0"
  }
}
